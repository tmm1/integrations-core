name: Enrico_Experiment
files:
- name: enrico_experiment.yaml
  options:
  - template: init_config
    options:
    - template: init_config/default
  - template: instances
    options:
    - template: instances/default
